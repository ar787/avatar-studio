import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#7c4dff',
      light: '#9670FF',
      dark: '#5635B2',
    },
    customAction: {
      menuHover: '#ddeaf814',
    },
  },
});
